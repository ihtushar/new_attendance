// Placeholder for Google Sheets integration
// In a real setup you would use the googleapis package and a service account

async function addAttendanceRow(email, status) {
  // TODO: Implement real Google Sheets API call
  console.log(`Would append row for ${email} with status ${status}`);
}

module.exports = { addAttendanceRow };
